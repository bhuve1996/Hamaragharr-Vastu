{% comment %}
  Single product card everywhere: delegates to vastu-grid-card.
  Usage: {% render 'vastu-product-card', product: product, section: section, settings: settings, block: block %}
  Styling: vastu-grid-carousel.css only.
{% endcomment %}
{% render 'vastu-grid-card', product: product, section: section, settings: settings %}
